import { Routes } from '@angular/router';

import { MarketShellComponent } from './market-shell.component';

export const MARKETS_ROUTES: Routes = [
  { path: '', component: MarketShellComponent },
];
